[project]
name = "cli"
version = "0.1.0"
description = "Auction House CLI"
authors = [
    {name = "Your Name",email = "you@example.com"}
]
requires-python = ">=3.10"
dependencies = [
    "click (>=8.0.0,<9.0.0)",
    "requests (>=2.31.0,<3.0.0)"
]

[project.scripts]
auction-cli = "main:cli"